# ============================================================
#  根目录 .gitignore
#
#  为什么还需要这一层：
#  backend/ 与 admin/ 各自已有 .gitignore，但 .gitignore 只对
#  **自己所在目录及其子目录**生效 —— 根目录这一层的文件没人管，
#  所以 .DS_Store、.idea/、.workbuddy/ 会被漏进来。
# ============================================================

# ---------- macOS 系统文件 ----------
.DS_Store
.AppleDouble
.LSOverride
._*

# ---------- IDE 配置 ----------
# .idea/ 里含本机绝对路径（JDK 位置、模块路径），
# 且每个人的 IDE 版本与插件不同，进仓库只会互相干扰。
.idea/
*.iml
*.iws
*.ipr
.vscode/

# ---------- 日志 ----------
*.log
logs/

# ---------- WorkBuddy 本地工作数据 ----------
# 会话记录与记忆文件属于本地状态，不是项目源码。
.workbuddy/

# ---------- 证书与密钥（红线，双保险） ----------
# backend/.gitignore 已经排除了 *.key / .env / application-local.yml，
# 这里再兜一层：万一将来密钥放在 devops 或根目录也不会漏出去。
*.pem
*.key
*.p12
*.jks
*.keystore

# ---------- 私人笔记（不进版本库） ----------
# 这两份是本地工作流文档，与项目源码无关：
#   CODEBUDDY.md            —— 协作约定 + 本机环境备忘（含 MySQL 端口、JDK 路径等）
#   docs/专家团-调用手册.md   —— WorkBuddy 里的 AI 工具调度手册
# ⚠️ 注意：CODEBUDDY.md 是项目唯一指令源，排除后 clone 到新机器需另备一份。
/CODEBUDDY.md
docs/专家团-调用手册.md
